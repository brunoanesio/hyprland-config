(defwidget power[]
  (box 
    :class "icon"
    :space-evenly false
    (eventbox
      :onclick "wlogout -p layer-shell &"
      (label 
      :class "power-icon"
      :style "font-size: 1.1rem"
      :tooltip "Logout"
      :text ""))))
